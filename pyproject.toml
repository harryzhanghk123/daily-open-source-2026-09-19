[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "planmaker-20260919"
version = "0.1.0"
description = "A dependency-free CLI for prioritizing tasks"
requires-python = ">=3.10"
